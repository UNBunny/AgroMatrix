# Comma-separated list of region codes for which weather is fetched from Open-Meteo.
# Leave empty to fetch weather for ALL regions (may hit 429 rate limits).
# Example: only Omsk and nearby regions
VITE_WEATHER_REGIONS=OMS,NVS,TOM,TYU,KGN,ALT
